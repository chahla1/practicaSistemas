
#include <stdio.h>

typedef struct {
    int arrInt[10];  // Aqui tenemos el array de 10 posiciones
    int arrSize;     // Añadimos el numero de elementos
    int arrAdd;      // Añadimos la suma de elementos
} arrayLength_t;